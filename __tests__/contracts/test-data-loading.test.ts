import { Data } from '../../../lib/types';

describe('Contract: Data Loading', () => {
  it('should load valid data structure from JSON', async () => {
    // TODO: Load sample JSON and validate against Data type
    // Expect failure until implementation
    expect(false).toBe(true);
  });
});
